services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: edu-one-ai:latest
    container_name: edu-one-ai
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
